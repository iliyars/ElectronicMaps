<Window x:Class="ElectronicMaps.WPF.Features.Workspace.Components.CreateComponentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ElectronicMaps.WPF.Features.Workspace.Components"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        Height="800" Width="600">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 1. Имя компонента -->
        <GroupBox Grid.Row="0"
                  Header="Основные данные"
                  Margin="0,0,0,10">
            <StackPanel>
                <Label Content="Имя компонента:" />
                <TextBox Text="{Binding ComponentName, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
        </GroupBox>

        <!-- 2. Контент (семейство + компонент) -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- TODO: Добавить UI для семейства и параметров -->
                <TextBlock Text="TODO: UI для выбора семейства и заполнения параметров" />
            </StackPanel>
        </ScrollViewer>

        <!-- 3. Статус -->
        <TextBlock Grid.Row="2"
                   Text="{Binding StatusMessage}"
                   Foreground="Gray"
                   Margin="0,10" />

        <!-- 4. Кнопки -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="Сохранить"
                    Command="{Binding SaveCommand}"
                    Width="100"
                    Margin="0,0,10,0" />
            <Button Content="Отмена"
                    Command="{Binding CancelCommand}"
                    Width="100" />
        </StackPanel>

        <!-- Overlay для IsBusy -->
        <Grid Grid.RowSpan="4"
              Background="#80000000"
              Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center"
                        HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True"
                             Width="200"
                             Height="20" />
                <TextBlock Text="{Binding StatusMessage}"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,10,0,0" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
